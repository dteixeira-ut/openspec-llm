## 1. Scaffolding — new deck module + type extension

- [x] 1.1 Extend `Theme.name` union in `apps/presentation/src/types.ts` from `'workflow' | 'research'` to `'workflow' | 'research' | 'package-extraction'`. No other type changes.
- [x] 1.2 Extend `Deck` union in `apps/presentation/src/hooks/useHashRoute.ts` from `'workflow' | 'research' | 'landing'` to `'workflow' | 'research' | 'landing' | 'package-extraction'`. Add a parser branch: when the first hash segment is `package-extraction`, return `{ deck: 'package-extraction', density: 'full' }`. Update the JSDoc route table to include the new route.
- [x] 1.3 Create `apps/presentation/src/decks/package-extraction/theme.ts` exporting a `Theme` const named `packageExtractionTheme` with `name: 'package-extraction'`, reusing `bg-ut-navy-dark` + `stage-glow` stage classes (matching workflow), a `rounded-2xl` card shape, an accent class that renders a solid `ut-teal` bar (`bg-ut-teal h-1.5 w-full`), and otherwise mirroring the workflow theme's typography and padding choices.
- [x] 1.4 Create `apps/presentation/src/decks/package-extraction/slides.ts` exporting `packageExtractionSlides: Slide[]` (note: `Slide`, not `ResearchSlide` — no `density` field). Include the 10–14 slides per the narrative arc in design.md Decision 1; see tasks 1.5–1.10 for the per-section content.
- [x] 1.5 Author Frame section (1–2 slides): title slide naming the work, and a problem-statement slide that frames the gap (templates exist in one repo; consuming repos can't pin a version). Cite the foundational mitigations in `notes`.
- [x] 1.6 Author Locked Decisions section (2–3 slides): one slide per cluster of decisions from memory `opsx-package-extraction-open-questions`. Use `bullets` for the decision list and a `callout` of tone `evidence` naming the memory record. Quote the locked decisions verbatim: scope `@usertesting/`, package name `insight-out-opsx` (working), UT private org registry, `opsx init` full bootstrap, Codex stays global per `codex-placement-matches-upstream`.
- [x] 1.7 Author The Bin section (2–3 slides): one slide introducing `opsx init` and `opsx sync` as subcommands of one bin; one slide showing the `opsx init` before/after via a `diff` content item (before: empty repo; after: `openspec/` scaffold + per-tool command dirs + commented-out `openspec/config.yaml` stub). Include a `callout` reminding readers that `openspec` is a hard peer dependency.
- [x] 1.8 Author Versioning + Drift section (2 slides): one slide stating the semver policy (patch / minor / major classes with concrete examples per design.md Decision 5 of the underlying extraction work); one slide stating the two-layer drift gate (package repo fixtures + consumer-repo install) and the Codex-global scope gap.
- [x] 1.9 Author Adoption section (1–2 slides): one slide naming `openspec-llm` as the first consumer (dogfooded before any external repo); one slide showing the one-line install + init flow available to external UT repos (`npm install @usertesting/insight-out-opsx && npx opsx init`).
- [x] 1.10 Author Close slide (1 slide): cite the foundational mitigations via `link` content items pointing at the archived `single-source-opsx-templates` and `tool-agnostic-opsx-templates` directories under `openspec/changes/archive/RAD-75634/`. Include a short framing line about the deck being the next phase of the same loop.

## 2. Wiring — route + landing-page card

- [x] 2.1 In `apps/presentation/src/App.tsx`, add imports for `packageExtractionSlides` and `packageExtractionTheme`, then add a new conditional branch: `if (route.deck === 'package-extraction') return <DeckView slides={packageExtractionSlides} theme={packageExtractionTheme} />`. Place the branch after the workflow branch and before the research-deck filter, preserving order.
- [x] 2.2 In `apps/presentation/src/components/LandingPage.tsx`, change the framing copy from "Two decks. One workflow." to "Three decks. One workflow." and update the supporting paragraph to name the third deck in one sentence (the package-extraction pitch). Preserve the workflow and research cards byte-for-byte aside from any grid-class changes needed in 2.3.
- [x] 2.3 In `apps/presentation/src/components/LandingPage.tsx`, change the cards' grid wrapper from `md:grid-cols-2` to `md:grid-cols-3`. Append a third card (after the research card) linking to `/#/package-extraction`, titled "Package Extraction" (or equivalent), using the same `rounded-2xl bg-white shadow-2xl` shape as the workflow card with a solid `ut-teal` top stripe. Include a one-line description naming the pitch (e.g., "The next phase: extract templates/opsx + bin/opsx-sync into a versioned npm package consuming repos can pin.").

## 3. Verification

- [x] 3.1 Run `cd apps/presentation && npm run build` to confirm the TypeScript compiles cleanly with the new union members and the new slide / theme modules.
- [x] 3.2 Manually count the rendered package-extraction deck slide list — it MUST land between 10 and 14 (target ~12).
- [x] 3.3 Spot-check that every slide asserting a locked decision includes either a `callout` of tone `evidence` or a `notes` line citing `opsx-package-extraction-open-questions` (or `codex-placement-matches-upstream` for the Codex-global claim).
- [x] 3.4 Manually verify routing: open `/#/package-extraction` in the dev server and confirm it renders with the new theme; open `/#/workflow` and `/#/research` and confirm they render unchanged; open `/#/package-extraction/summary` and confirm it falls through to the landing page (or to whatever the existing unrecognized-hash behavior is).
- [x] 3.5 Manually verify landing-page layout at desktop (>= md breakpoint): three cards side-by-side, package-extraction third. Then resize the viewport below the md breakpoint and verify single-column stacking still works for all three cards.
- [x] 3.6 Run `openspec validate add-package-extraction-deck --strict` and confirm success.
- [x] 3.7 Run `openspec validate --all` and confirm no other specs regressed.
- [x] 3.8 Run `node bin/opsx-sync --check` to confirm the templates-side of the repo is still clean (this change should not touch any opsx template).
