## 1. Slide additions and edits

- [x] 1.1 Add two new archive URL constants at the top of `apps/presentation/src/decks/research/slides.ts`: `SINGLE_SOURCE_ARCHIVE_URL` and `TOOL_AGNOSTIC_ARCHIVE_URL`, pointing at `https://github.com/dteixeira-ut/openspec-llm/tree/main/openspec/changes/archive/RAD-75634/single-source-opsx-templates` and `.../tool-agnostic-opsx-templates` respectively.
- [x] 1.2 Update the header doc block (slides.ts L11–14) to enumerate mitigations 1–4 with their archive paths.
- [x] 1.3 Update the Section 4 comment from `// ─── Section 4 — MITIGATIONS SHIPPED (4 slides) ───` to `... (8 slides) ...`.
- [x] 1.4 Insert slide `mitigation-single-source-overview` immediately after `mitigation-skills-graduation`. Density: `'both'`. Bullets cover: (a) three drifted trees collapsed to one canonical `templates/opsx/` directory, (b) `bin/opsx-sync` generator fans out to Claude/Cursor/Codex command paths, (c) CI drift-check at `.github/workflows/opsx-template-drift.yml`, (d) auto-invocation preserved via rich `description:` frontmatter carried verbatim. Include `link` to `SINGLE_SOURCE_ARCHIVE_URL`. Notes section explains the slide's role in the section.
- [x] 1.5 Insert slide `mitigation-single-source-architecture` immediately after `mitigation-single-source-overview`. Density: `'full'`. Use `bullets` + `callout` (evidence tone) quoting the living spec's "no other location SHALL contain hand-authored opsx workflow bodies" requirement. Cover: per-tool output paths, the canonical-source rule, the CI drift-check covering Claude + Cursor scopes (Codex global path is a known scope gap, called out in passing). Notes explain why we keep Codex global to match upstream openspec.
- [x] 1.6 Insert slide `mitigation-tool-agnostic-overview` immediately after `mitigation-single-source-architecture`. Density: `'both'`. Bullets cover: (a) 7 of 11 templates rewritten in tool-agnostic prose, (b) 28 Claude-specific tool references removed from bodies, (c) HTML-comment affordance hints introduced as the only Claude-specific surface, (d) generator warn extended with a Claude-isms scan. Include `link` to `TOOL_AGNOSTIC_ARCHIVE_URL`.
- [x] 1.7 Insert slide `mitigation-tool-agnostic-affordances` immediately after `mitigation-tool-agnostic-overview`. Density: `'full'`. Use `text` intro, then a `diff` content item showing before/after for a representative step (e.g., the apply discrete-option prompt). The `before` shows `use the **AskUserQuestion tool** to let the user select`; the `after` shows the single-line affordance hint plus the tool-agnostic prose. Add a second short bullet list covering the block-level shape used by `pr.md`'s polling loop and the augment-never-replace rule. Include a `callout` (evidence tone) referencing the spec's "Block-level hint missing a tool-agnostic fallback" scenario as the generator-enforced safety net.
- [x] 1.8 Update the `close-dogfooding` slide's first bullet (currently "The two mitigations shipped before this deck was authored. The author was a /opsx:apply agent reading the artifacts.") to: "Two of the four mitigations shipped before this deck was authored; two more (`single-source-opsx-templates`, `tool-agnostic-opsx-templates`) shipped AFTER, because the deck itself surfaced gaps the original case study missed. The author of each round was a /opsx:apply agent reading the artifacts of the prior round." Keep every other field (title, callout, link, notes) untouched.

## 2. Verification

- [x] 2.1 Run `cd apps/presentation && npm run build` to confirm the slide-array TypeScript compiles cleanly with the new slides.
- [x] 2.2 Manually count the rendered full-deck slide list — it MUST land between 24 and 34 (target ~28) and include the four mitigation overview titles by id: `mitigation-harden-overview`, `mitigation-skills-overview`, `mitigation-single-source-overview`, `mitigation-tool-agnostic-overview`.
- [x] 2.3 Manually count the summary-deck slide list (slides with `density: 'both'` or `density: 'summary'`) — it MUST land between 9 and 17 (target ~13).
- [x] 2.4 Spot-check the close slide rendering: the dogfooding bullet must reference all four mitigations explicitly and call out the before/after chronology.
- [x] 2.5 Run `openspec validate sync-research-deck-with-templates-work --strict` and confirm success.
- [x] 2.6 Run `openspec validate --all` and confirm no other specs regressed.
- [x] 2.7 Run `node bin/opsx-sync --check` to confirm the templates-side of the repo is still clean (this change should not touch any template).
